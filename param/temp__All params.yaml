global_costmap: {
  global_frame: map,
  robot_base_frame: base_link,
  update_frequency: 10.0,
  # publish_frequency
  static_map: true,
  transform tolerance: 0.5,
  obstacle_range: 1.5,
  raytrace_range: 2.0,
  # footprint: [[-0.15, -0.15], [-0.15, 0.15], [0.15,-0.15], [0.15,0.15]],
  robot_radius: 0.1,
  inflation_radius: 0.6,

  observation_sources: laser_scan_sensor,
  observation_persistence: 0.0,

  laser_scan_sensor: {sensor_frame: base_scan, data_type: LaserScan, topic: scan, marking: true, clearing: true}
  }

local_costmap: {
  global_frame: odom,
  robot_base_frame: base_link,
  update_frequency: 10.0,
  publish_frequency: 10.0,
  static_map: false,
  rolling_window: true,
  width: 6.0,
  height: 6.0,
  resolution: 0.1,
  transform_tolerance: 0.5,
  obstacle_range: 1.5,
  raytrace_range: 2.0,
  # footprint: [[-0.15, -0.15], [-0.15, 0.15], [0.15,-0.15], [0.15,0.15]],
  robot_radius: 0.1,
  inflation_radius: 0.3,

  observation_sources: laser_scan_sensor,
  observation_persistence: 0.0,

  laser_scannsor: {sensor_frame: base_scan, data_type: LaserScan, topic: scan, marking: true, clearing: true}
}
